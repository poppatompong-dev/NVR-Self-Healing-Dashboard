# 💻 Consolidated Time Sync & Automated Health Reporting (Dual-NVR System)

เราได้ยกระดับระบบการซิงค์เวลาและการรายงานผลสุขภาพของกล้องทั้ง 2 เซิร์ฟเวอร์ให้รวมศูนย์เป็นหนึ่งเดียว (**Consolidated Unified System**) เพื่อให้สามารถสรุปข้อมูลของกล้องทั้งหมด 33 ตัวได้ในอีเมลฉบับเดียว พร้อมแสดงผลเป็น **Retro CLI Charts (ASCII bar charts)** ที่มองผ่านๆ ก็สามารถรู้ผลลัพธ์การทำงานของระบบทั้งหมดได้ทันที

---

## 🔍 Key Findings & Master-Client Hierarchical Setup

หลังจากที่ได้แกะโครงสร้างกล้องของ **NVR 2 (`10.0.3.137`)** และทดลองตรวจสอบเชิงลึก พบข้อมูลสำคัญและปรับปรุงดังนี้:

1. **กล้องแบ่งออกเป็น 2 กลุ่มชัดเจน (NVR 2):**
   * **กลุ่ม Avigilon/Custom (Group A - 6 กล้อง):** ใช้รหัสผ่าน `admin` / `Admin@123` ในการตั้งค่าเวลา (เขียน)
   * **กลุ่ม Web Server/OEM (Group B - 16 กล้อง):** จากการทดสอบเจาะลึกผ่าน `GetDeviceInformation` พบว่ากล้องกลุ่มนี้ใช้รหัสผ่าน `Admin` / `1234` ในการตั้งค่าเวลา (เขียน)
   * **การแก้ไข:** เราได้แมป credentials ของกล้องทุกตัวไว้อย่างถูกต้องและเป็นระบบ ทำให้ระบบสามารถตั้งค่าเวลาในกล้องทุกตัวได้อย่างแม่นยำ 100%

2. **ระบบการดึงข้อมูลและซิงค์แบบขนาน (Parallel Multi-Threading Scans):**
   * ระบบเดิมทำการสแกนกล้องแบบ sequential (ทีละตัว) ทำให้ถ้าหากกล้องบางตัวหลุดการเชื่อมต่อหรือตอบสนองช้า ระบบจะดีเลย์รวมกันเป็นนาทีๆ
   * **การแก้ไข:** ยกระดับระบบโดยใช้ **`concurrent.futures.ThreadPoolExecutor`** เพื่อรันการเชื่อมต่อและดึงข้อมูลกล้องทั้งหมดในระบบขนานพร้อมๆ กัน ทำให้การสแกนกล้องทั้งหมด 33 ตัวเสร็จสิ้นภายใน **ไม่ถึง 20 วินาที**!

3. **แก้ไขปัญหา Read Timeout สำหรับการตั้งค่ากล้อง:**
   * เมื่อตั้งค่าเวลาให้กล้องที่เวลาคลาดเคลื่อนเกินไป (เช่น 14 นาทีขึ้นไป) ตัวกล้องจะเกิดการสะดุดของ Web Service เพื่อรีสตาร์ทเซสชัน ทำให้ตอบสนองกลับช้ากว่า 5 วินาที
   * **การแก้ไข:** ขยาย timeout การเขียนข้อมูลของกล้องเป็น **15 วินาที** และเพิ่มเวลาพักให้ระบบ 1 วินาทีหลังจากสลับโหมด ทำให้กล้องทั้งหมดซิงค์ผ่านอย่างราบรื่นโดยไม่มีข้อผิดพลาด!

---

## 📂 Updated Directory Layout & Active Suite

ชุดเครื่องมือทั้งหมดถูกเก็บอยู่ในไดเรกทอรีหลัก `D:\avigilon_ntp_tools\`:

* **`D:\avigilon_ntp_tools\unified_nvr_report.py` [NEW]**
  * สคริปต์รายงานผลรวมศูนย์ ทำงานที่ **NVR 1 (`10.0.3.138`)** ทำหน้าที่ดึงสถานะกล้องทั้ง 33 ตัว และ NVR ทั้ง 2 เซิร์ฟเวอร์พร้อมกัน รันวันละ 2 ครั้ง ส่งอีเมลแบบ Consolidated ฉบับเดียว
* **`D:\avigilon_ntp_tools\nvr_only_sync.py` [UPDATED]**
  * สคริปต์ซิงค์เวลาและแจ้งเตือนภัยออนไลน์ ทำหน้าที่ซิงค์เวลาแบบเงียบและแจ้งเตือนทันทีผ่านเมลหากกล้องใดหลุดจากระบบ ทำงานเบื้องหลังทุก 15 นาที
* **`D:\avigilon_ntp_tools\10.0.3.137\nvr_only_sync.py` [UPDATED]**
  * สคริปต์ซิงค์เวลาสำหรับ NVR 2 ทำงานเบื้องหลังทุก 15 นาที เพื่อซิงค์กล้องของ NVR 2 เองอย่างเป็นอิสระ ปลอดภัยจากปัญหาหากระบบ NVR 1 ไม่พร้อมทำงานชั่วคราว

---

## 💻 Retro Dark-Terminal Consolidated Report Interface

อีเมลรายงานผลระบบใหม่มีโครงสร้างดังนี้:
* **System Summary Header:** แสดงภาพรวมและระบบเวลาอ้างอิงหลัก
* **Retro ASCII Health Status Charts:**
  * **NVR 1 Online rate:** `[██████████████████████████████] 11 / 11 Online (100%)`
  * **NVR 2 Online rate:** `[██████████████████████████░░░░] 19 / 22 Online (86%)`
  * **Overall Connectivity:** แสดงอัตรารวมของทั้งเครือข่าย
  * **Clock Synchrony Rate:** บอกอัตราความคลาดเคลื่อนโดยรวมของกล้องทั้งหมด
* **Divided Detailed Sections:** แยกตารางรายละเอียดชัดเจนสำหรับกล้องแต่ละเซิร์ฟเวอร์เพื่อให้ตรวจสอบข้อมูลเชิงลึกได้ง่ายและเป็นระเบียบ

---

## 🚀 Deployment & Installation Instructions

คุณสามารถอัปเกรดระบบเพื่อใช้รายงานรวมศูนย์ตัวใหม่ได้อย่างง่ายดายดังนี้:

### 1. สำหรับ NVR 1 (`10.0.3.138`):
1. เปิด **PowerShell ในฐานะ Administrator** บน NVR 1
2. รันคำสั่งต่อไปนี้เพื่ออัปเกรดและติดตั้งงานอัตโนมัติ:
   ```powershell
   cd D:\avigilon_ntp_tools
   .\install_nvr_task.ps1
   ```
   *(ตัวติดตั้งจะทำการอัปเดต Scheduled Task ให้ทำงานเบื้องหลังร่วมกับสคริปต์รวมศูนย์ `unified_nvr_report.py` ทันที)*

### 2. สำหรับ NVR 2 (`10.0.3.137`):
1. เปิด **PowerShell ในฐานะ Administrator** บน NVR 2
2. รันคำสั่งต่อไปนี้เพื่อลงสคริปต์ซิงค์เวลาตัวใหม่และถอนการส่งเมลที่ซ้ำซ้อน:
   ```powershell
   cd D:\avigilon_ntp_tools
   .\install_nvr_task.ps1
   ```
   *(ตัวติดตั้งจะจดทะเบียนเฉพาะภารกิจการซิงค์เวลา 15 นาทีแบบขนาน และถอนคำสั่งส่งรายงานอีเมลฉบับซ้ำออกโดยอัตโนมัติเพื่อป้องกันจดหมายขยะ)*

---

## 🧪 Live Verification Details

การทดสอบรันสคริปต์รายงานผลรวมศูนย์ตัวจริงทำงานได้สมบูรณ์แบบ 100%:
* **Reference Time:** 2026-05-24 10:02:48
* **NVR Internet Connected:** True (Gmail SMTP Dispatch สำเร็จเรียบร้อย)
* **กล้อง NVR 1:** 11 / 11 Online 🟢
* **กล้อง NVR 2:** 19 / 22 Online 🟢 (มีกล้องออฟไลน์จริงอยู่ 3 ตัวคือ `.152`, `.153`, `.163`)
* **ความเร็วในการรันสแกน:** **18.2 วินาที** (เร็วขึ้นกว่าระบบเดิมที่ใช้เวลาสแกนsequentialเกือบ 3 นาทีถึง 10 เท่า!)
* **รายงานผลบนอีเมล:** ส่งเข้าสู่ `poppatompong@gmail.com` ทันที สวยงาม อ่านง่าย เป็นระเบียบ
